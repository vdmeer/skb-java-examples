<?xml version="1.0" encoding="UTF-8"?>
	<project name="common-standard" xmlns:s="standard.artifact.compile">

	<import file="../common/common.xml"/>

	<!-- Macros -->
	<macrodef name="do-compile">
		<attribute name="classpath.id"/>
		<attribute name="input.dir"/>
		<attribute name="output.dir"/>
		<attribute name="resources.dir"/>
		<sequential>
			<echo message="Source ${source.version}, Target ${target.version}"/>
			<s:javac destdir="@{output.dir}" source="${source.version}" target="${target.version}" debug="${compile.debug}" debugLevel="${compile.debug.level}" includeAntRuntime="true">
				<compilerarg line="${compiler.args}"/>
				<src path="@{input.dir}"/>
				<include name="**/*.java"/>
				<include name="*.java"/>
				<classpath refid="@{classpath.id}"/>
				<compilerclasspath>
					<path refid="ecj.classpath"/>
				</compilerclasspath>
			</s:javac>
			<copy todir="@{output.dir}">
				<fileset dir="@{input.dir}" erroronmissingdir="false">
					<exclude name="**/*.java"/>
					<exclude name="*.java"/>
				</fileset>
			</copy>
			<copy todir="@{output.dir}">
				<fileset dir="@{resources.dir}" erroronmissingdir="false"/>
			</copy>
		</sequential>
	</macrodef>

</project>
