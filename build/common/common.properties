# Common Properties
lib.dir=${buildsystem.home.dir}/lib
artifacts.dir=${target.dir}/artifacts
target.dir=${basedir}/target
ci.build=false
natural.name=${ant.project.name}
build.stamp=BUILD-${timestamp}
# Integration build stamps
#bundle.version=${version}.${build.stamp}
# Release versions
bundle.version=${version}

###publish.bucket=repository.springsource.com
local.repo.dir=${env.IVY_HOME}/repository/downloaded
integration.repo.dir=${env.IVY_HOME}/repository/integration-repo
ivy.cache.dir=${env.IVY_HOME}/repository/ivy-cache
runtime.bundle.dir=${env.RUNTIME_HOME}/build.bundles
local.build=true

# Artifact Properties
main.java.dir=${basedir}/src/main/java
main.resources.dir=${basedir}/src/main/resources
main.output.dir=${target.dir}/classes

# Javadoc Properties
javadoc-all.output.dir=${target.dir}/all
javadoc-api.output.dir=${target.dir}/api
javadoc.prep.dir=${target.dir}/javadoc-prep
javadoc.exclude.package.names=
javadoc.stylesheet.file=${buildsystem.home.dir}/common/javadoc.css
javadoc.resources.dir=${buildsystem.home.dir}/common/javadoc-resources
javadoc.max.memory=128M
javadoc-bundle.output.dir=${target.dir}/javadoc-bundle

# JAR Properties
implementation.title=${ant.project.name}
implementation.version=${bundle.version}
jar.output.dir=${artifacts.dir}
jar.output.lib.dir=${target.dir}/lib
jar.output.file=${jar.output.dir}/${ant.project.name}.jar
jar.output.complete.file=${jar.output.dir}/${ant.project.name}-complete.jar
source-jar.output.dir=${artifacts.dir}
source-jar.output.file=${source-jar.output.dir}/${ant.project.name}-sources.jar
javadoc-jar.output.dir=${artifacts.dir}
javadoc-jar.output.file=${javadoc-jar.output.dir}/${ant.project.name}-javadoc.jar

# Compiler Properties
org.eclipse.jdt.core.compiler.ecj.version=4.3.1
build.compiler=org.eclipse.jdt.core.JDTCompilerAdapter
compile.debug=true
compile.debug.level=lines,vars,source
compiler.args=-enableJavadoc -encoding UTF-8

# Package Properties
copy.to.dir=${target.dir}/ext/lib
package.dir=${target.dir}/package-expanded
package.release.dir=${package.output.dir}/../../releases
package.basename=${natural.name}-${bundle.version}
package.file.name=${package.basename}.zip
package.output.dir=${package.dir}/${package.basename}
package.output.file=${artifacts.dir}/${package.file.name}
package.dist.dir=${package.output.dir}/dist
package.lib.dir=${package.output.dir}/../../lib
package.src.dir=${package.output.dir}/src
package.docs.dir=${package.output.dir}/docs
package.docs-expanded.dir=${package.output.dir}/javadocs
package.pom.output.file=${ant.project.name}-${bundle.version}.pom
package.jar.output.file=${ant.project.name}-${bundle.version}.jar
package.source-jar.output.file=${ant.project.name}-${bundle.version}-sources.jar
package.javadoc-bundle.output.file=${ant.project.name}-${bundle.version}-javadoc.jar

# Test Properties
test.java.dir=${basedir}/src/test/java
test.resources.dir=${basedir}/src/test/resources
test.output.dir=${target.dir}/test-classes
test-results.output.dir=${target.dir}/test-results
test.vm.args=

# Clover Properties
clover.db.file=${clover.output.dir}/db/clover.db
clover.output.dir=${target.dir}/clover
clover.staging.dir=${clover.output.dir}/staging
main.clover.dir=${clover.staging.dir}/main
test.clover.dir=${clover.staging.dir}/test
com.springsource.com.cenqua.clover.version=2.4.2
clover.enforce=false
clover.coverage=0%

# Findbugs Properties
findbugs.dir=${lib.dir}/findbugs
findbugs.exclude.file=${buildsystem.home.dir}/common/findbugs-exclude.xml
findbugs.output.dir=${target.dir}/findbugs
findbugs.output.file=${findbugs.output.dir}/${ant.project.name}.xml
edu.umd.cs.findbugs.version=1.3.7

# Ivy Properties
bucket.basename=springframework.org
release.type=integration
ivy.checksums=sha1,md5
ivy.deliver.revision=${bundle.version}
ivy.file=${basedir}/ivy.xml
ivy.lib.dir=${lib.dir}/ivy
ivy.output.dir=${artifacts.dir}
ivy.output.file=${ivy.output.dir}/ivy.xml
ivy.settings.file=${buildsystem.home.dir}/common/ivysettings.xml

# Maven Properties
pom.output.dir=${artifacts.dir}
pom.output.file=${pom.output.dir}/${ant.project.name}.pom
local.pom.output.file=${basedir}/${ant.project.name}.pom
org.apache.maven.ant.tasks.version=2.1.3
maven.version=3.0.3

# Jar Manifest Properties
manifest.mainclass=
manifest.buildby=vdmeer

# Package Distribution File Defaults
package.dist.license.file=${buildsystem.home.dir}/common/default-files/LICENSE
package.dist.readme.file=${buildsystem.home.dir}/common/default-files/README
package.dist.changelog.file=${buildsystem.home.dir}/common/default-files/CHANGELOG

# Project Properties
project.demos.home=${env.PROJECT_HOME}/demos