<?xml version="1.0" encoding="UTF-8"?>
<project name="build-distro" xmlns:ivy="antlib:org.apache.ivy.ant" default="distro" >
	<!-- List all the bundles, you wish to include in the build process here -->
	<!-- NOTE: Beware the build order -->

	<path id="bundles">
		<pathelement location="../projects/examples"/>
	</path>

	<property environment="env"/>
	<property file="${env.PROJECT_HOME}/build/build.properties"/>
	<property file="${env.PROJECT_HOME}/build/build.versions"/>

	<import file="${env.PROJECT_HOME}/build/project/top-level.xml"/>
	<import file="${env.PROJECT_HOME}/build/package/package-top-level.xml"/>

	<import file="${env.PROJECT_HOME}/build/multi-bundle/default.xml"/> 

	<!--<target name="distro" depends="prepare,clean,jar,javadoc-api,package"/>-->
	<target name="distro" depends="prepare,clean,jar,package"/>
</project>
